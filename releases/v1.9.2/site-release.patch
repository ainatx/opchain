--- a/site/src/pages/changelog.astro
+++ b/site/src/pages/changelog.astro
@@ -161,7 +161,7 @@
           aria-selected="true"
           tabindex="0"
         >
-          Just Released <span class="tab-count">19 shipped</span>
+          Just Released <span class="tab-count">20 shipped</span>
         </button>
         <button
           class="tab-btn tab-btn--next"
@@ -200,7 +200,7 @@
         <span class="panel-sub">Latest release is open; patches and earlier releases expand in place.</span>
       </div>
 
-      <!-- v1.9 — latest shipped release line (v1.9.0 → v1.9.1); "Assurance and governed delivery ops". Deep-linkable from #v1-9. -->
+      <!-- v1.9 — latest shipped release line (v1.9.0 → v1.9.2); "Assurance and governed delivery ops". Deep-linkable from #v1-9. -->
       <article class="hero-card hero-card--released is-open" id="v1-9" data-card>
         <button
           class="hero-head"
@@ -211,7 +211,7 @@
         >
           <span class="hero-chevron" aria-hidden="true">▶</span>
           <span class="hero-badge">latest release</span>
-          <span class="hero-ver">v1.9.0 → v1.9.1 · Sep 02 – Sep 13, 2026</span>
+          <span class="hero-ver">v1.9.0 → v1.9.2 · Sep 02 – Sep 13, 2026</span>
           <span class="hero-title">Assurance and governed delivery ops</span>
           <span class="hero-desc">
             Delivery evidence you can review and enforce. v1.9 adds four skills
@@ -253,6 +253,28 @@
               pre-v1.9 hosted checkpoint records are deleted during rollout;
               local on-disk checkpoints are unaffected.
             </div>
+          </div>
+        </div>
+      </article>
+
+      <!-- v1.9.2 — verified handoffs; apply only in the post-tag site release. -->
+      <article class="rel-card" id="v1-9-2" data-card>
+        <button class="rc-row" type="button" aria-expanded="false" aria-controls="v1-9-2-body" data-disclosure-toggle>
+          <span class="rc-toggle" aria-hidden="true">&#9654;</span>
+          <span class="rc-meta">
+            <span class="rc-head"><span class="ver-pill ver-pill--past">v1.9.2 patch</span><span class="rc-title">Verified handoffs</span><span class="rc-date">Sep 13, 2026</span></span>
+            <span class="rc-summary">Commit checks follow the staged candidate; local state survives restarts; skills exchange validated evidence.</span>
+          </span>
+        </button>
+        <div class="card-body" id="v1-9-2-body" data-disclosure-body inert>
+          <div class="card-body-inner">
+            <p>The audit follow-through adds executable verification and reusable local tools across the existing 33 skills.</p>
+            <ul>
+              <li>Explicit repository enrollment verifies staged commits and blocks failed required checks.</li>
+              <li>Durable local MCP state, bundled references and typed handoffs connect the skill family.</li>
+              <li>Prompt/cost checks, private telemetry commands and optional execution kits have runnable local paths.</li>
+            </ul>
+            <div class="compat-box"><strong>Setup and migration required.</strong> Run <code>/oc-enroll</code> per repository, refresh local sessions and producer evidence, and opt in to telemetry locally. Local hooks are bypassable; hosted state is advisory. Kits are not sandboxes. Live provider and remote CI certification remain separate.</div>
           </div>
         </div>
       </article>
--- a/site/src/pages/styleguide.astro
+++ b/site/src/pages/styleguide.astro
@@ -307,7 +307,7 @@
       <div class="sg-row">
         <Badge variant="accent">Plan</Badge>
         <Badge variant="accent">Tri-agent</Badge>
-        <Badge variant="neutral">v1.9.1</Badge>
+        <Badge variant="neutral">v1.9.2</Badge>
         <Badge variant="success">passing</Badge>
         <Badge variant="danger">failing</Badge>
         <Badge variant="info">info</Badge>
--- a/site/src/pages/architecture.astro
+++ b/site/src/pages/architecture.astro
@@ -1299,7 +1299,7 @@
                human promoted or adopted rides the re-inject bus back into the
                next run.
 
-               NONE OF IT IS IN THE CATALOG YET. Today: 33 skills at 1.9.1 on
+               NONE OF IT IS IN THE CATALOG YET. Today: 33 skills at 1.9.2 on
                checkpoint wire 1.1. So every panel is badged PLANNED, the two
                skills link to /changelog#v2-0 rather than /skills/<id> (those
                pages do not exist), the Outcome Scorecard kit is drawn without
@@ -1322,7 +1322,7 @@
                grounds are luminance-identical (#12191f vs #1c1710 = 1.00:1) and
                differ only in hue, so the dashed border and the chrome — not the
                panel fill — are what separate the rail. Not on the spine. -->
-          <div class="lrail" role="group" aria-label="Learning rail (planned for v2.0): the self-improvement loop. Checkpoint wire 1.2 accumulates every run's eval scores, the Outcome Scorecard kit turns them into a decaying directive, oc-hindsight turns committed outcomes into promotable lessons, and oc-evolve turns recurring failures into adoptable rules. Each governed workflow ends at a human gate, and only what a human promoted or adopted is re-injected into the next run. None of it has shipped: the catalog today is 33 skills at 1.9.1.">
+          <div class="lrail" role="group" aria-label="Learning rail (planned for v2.0): the self-improvement loop. Checkpoint wire 1.2 accumulates every run's eval scores, the Outcome Scorecard kit turns them into a decaying directive, oc-hindsight turns committed outcomes into promotable lessons, and oc-evolve turns recurring failures into adoptable rules. Each governed workflow ends at a human gate, and only what a human promoted or adopted is re-injected into the next run. None of it has shipped: the catalog today is 33 skills at 1.9.2.">
             <div class="lrail-head">
               <span class="lrail-eyebrow">v2.0 · cross-cutting · learning rail · planned, not shipped</span>
               <span class="lrail-title">Self-improvement — measure the runs, remember the outcomes, change the behaviour; each step ends at a human</span>
@@ -1418,7 +1418,7 @@
 
                 <text x="380" y="214" fill="#a5bcd1" font-size="6.5" font-family="JetBrains Mono,monospace" text-anchor="middle" opacity="0.85">emitters harvested: oc-app-architect eval reports · oc-code-auditor audits · oc-monitoring-ops postmortems · oc-bug-check verdicts</text>
                 <text x="380" y="225" fill="#8cb9fc" font-size="6.5" font-family="JetBrains Mono,monospace" text-anchor="middle">★ = a human gate — interactive confirm plus a signed Approved-by: commit · not an audit gate, the count stays 3</text>
-                <text x="380" y="236" fill="#a5bcd1" font-size="5.5" font-family="JetBrains Mono,monospace" text-anchor="middle" opacity="0.85">planned for v2.0 — none of this is in the catalog yet; today it is 33 skills at 1.9.1 and checkpoint wire 1.1</text>
+                <text x="380" y="236" fill="#a5bcd1" font-size="5.5" font-family="JetBrains Mono,monospace" text-anchor="middle" opacity="0.85">planned for v2.0 — none of this is in the catalog yet; today it is 33 skills at 1.9.2 and checkpoint wire 1.1</text>
               </svg>
             </div>
             <div class="lrail-anchors">
@@ -1441,7 +1441,7 @@
                 <span class="ia-badges"><span class="ia-new">v2.0 · KIT, NOT A SKILL</span></span>
               </div>
             </div>
-            <div class="lrail-foot">↑ planned for v2.0 — nothing here has shipped · the catalog today is 33 skills at 1.9.1 on wire 1.1 · learned content is advisory data, never instructions, and never outranks a skill contract or repo policy</div>
+            <div class="lrail-foot">↑ planned for v2.0 — nothing here has shipped · the catalog today is 33 skills at 1.9.2 on wire 1.1 · learned content is advisory data, never instructions, and never outranks a skill contract or repo policy</div>
           </div>
 
           <!-- Legend -->
@@ -1618,7 +1618,7 @@
               <span>learning rail · v2.0 <span style="color:var(--learn)">planned</span></span>
               <div class="legend-card" role="tooltip">
                 <div class="legend-card-title" style="color:#8cb9fc">Learning rail (planned for v2.0)</div>
-                The only dashed rail on this page, because none of it has shipped — the catalog today is 33 skills at 1.9.1 on checkpoint wire 1.1. v2.0 closes a loop instead of adding two more boxes. <strong style="color:#8cb9fc">Wire 1.2</strong> is the release's one BREAKING change: <code>eval_scores[]</code> gains real append semantics, the in-file array becomes a bounded recent window, and the archive of record moves to <code>.checkpoints/history/&lt;skill&gt;.eval.ndjson</code>, so a reader wanting full history must union both. The <strong style="color:#8cb9fc">Outcome Scorecard kit</strong> is a kit, not a skill — it carries no CP/ORC badge and is not counted. <strong style="color:#8cb9fc">oc-hindsight</strong> harvests committed outcomes into source-cited lessons; <strong style="color:#8cb9fc">oc-evolve</strong> turns recurring failures into rules proven by oc-prompt-ops regressions. Both end at a ★ <em>human</em> gate — an interactive confirm plus a signed <code>Approved-by:</code> commit — which is not an audit gate, so the count stays three.
+                The only dashed rail on this page, because none of it has shipped — the catalog today is 33 skills at 1.9.2 on checkpoint wire 1.1. v2.0 closes a loop instead of adding two more boxes. <strong style="color:#8cb9fc">Wire 1.2</strong> is the release's one BREAKING change: <code>eval_scores[]</code> gains real append semantics, the in-file array becomes a bounded recent window, and the archive of record moves to <code>.checkpoints/history/&lt;skill&gt;.eval.ndjson</code>, so a reader wanting full history must union both. The <strong style="color:#8cb9fc">Outcome Scorecard kit</strong> is a kit, not a skill — it carries no CP/ORC badge and is not counted. <strong style="color:#8cb9fc">oc-hindsight</strong> harvests committed outcomes into source-cited lessons; <strong style="color:#8cb9fc">oc-evolve</strong> turns recurring failures into rules proven by oc-prompt-ops regressions. Both end at a ★ <em>human</em> gate — an interactive confirm plus a signed <code>Approved-by:</code> commit — which is not an audit gate, so the count stays three.
                 <div class="legend-card-examples">oc-hindsight · oc-evolve · the Outcome Scorecard kit · checkpoint wire 1.2</div>
               </div>
             </span>
--- a/site/src/components/MobileArchitecture.astro
+++ b/site/src/components/MobileArchitecture.astro
@@ -744,7 +744,7 @@
            rail's mobile parity. Same four nodes in one column, with the
            loop closed down the left gutter so "it comes back" survives the
            narrow layout. Nothing here has shipped: the catalog is 33 skills
-           at 1.9.1 on wire 1.1, so the badge reads PLANNED, no count moves,
+           at 1.9.2 on wire 1.1, so the badge reads PLANNED, no count moves,
            and the eyebrow stays v1.9 until the release cut. -->
       <details class="ma-band ma-lrail" open aria-label="Learning rail (planned for v2.0): checkpoint wire 1.2 accumulates every run's eval scores, the Outcome Scorecard kit turns them into a decaying directive, oc-hindsight turns committed outcomes into promotable lessons, and oc-evolve turns recurring failures into adoptable rules. Each ends at a human gate and only what a human promoted or adopted is re-injected. None of it has shipped.">
         <summary class="ma-band-header ma-lrail-header">
@@ -804,7 +804,7 @@
           </svg>
         </div>
         <div class="ma-band-caption">
-          <p><b>Planned for v2.0 — none of this has shipped.</b> The catalog today is 33 skills at 1.9.1 on checkpoint wire 1.1. v2.0 closes a loop rather than adding two more boxes. <b>Wire 1.2</b> is the one BREAKING change: <code>eval_scores[]</code> gains real append semantics, the in-file array becomes a bounded recent window, and the archive of record moves to <code>.checkpoints/history/</code>, so readers wanting full history must union both. The <b>Outcome Scorecard kit</b> is a repo-local kit, not a skill — it samples discarded Evaluator verdicts and emits a decaying directive once a decline recurs three times. <code>oc-hindsight</code> harvests committed outcomes — <code>oc-app-architect</code> eval reports, <code>oc-code-auditor</code> audits, <code>oc-monitoring-ops</code> postmortems, <code>oc-bug-check</code> verdicts — into source-cited lessons, with retrieval methodology from <code>oc-rag-forge</code>. <code>oc-evolve</code> clusters recurring failures into candidate rules and proves them through <code>oc-prompt-ops</code> regressions before a human adopts one; proven rules graduate upstream via <code>oc-git-ops</code> and <code>oc-release-ops</code>. The ★ gates are <em>human</em> gates — an interactive confirm plus a signed <code>Approved-by:</code> commit — not audit gates, so the ecosystem still has three.</p>
+          <p><b>Planned for v2.0 — none of this has shipped.</b> The catalog today is 33 skills at 1.9.2 on checkpoint wire 1.1. v2.0 closes a loop rather than adding two more boxes. <b>Wire 1.2</b> is the one BREAKING change: <code>eval_scores[]</code> gains real append semantics, the in-file array becomes a bounded recent window, and the archive of record moves to <code>.checkpoints/history/</code>, so readers wanting full history must union both. The <b>Outcome Scorecard kit</b> is a repo-local kit, not a skill — it samples discarded Evaluator verdicts and emits a decaying directive once a decline recurs three times. <code>oc-hindsight</code> harvests committed outcomes — <code>oc-app-architect</code> eval reports, <code>oc-code-auditor</code> audits, <code>oc-monitoring-ops</code> postmortems, <code>oc-bug-check</code> verdicts — into source-cited lessons, with retrieval methodology from <code>oc-rag-forge</code>. <code>oc-evolve</code> clusters recurring failures into candidate rules and proves them through <code>oc-prompt-ops</code> regressions before a human adopts one; proven rules graduate upstream via <code>oc-git-ops</code> and <code>oc-release-ops</code>. The ★ gates are <em>human</em> gates — an interactive confirm plus a signed <code>Approved-by:</code> commit — not audit gates, so the ecosystem still has three.</p>
           <div class="ma-bc-cp"><span class="ma-bc-cp-badge">CP</span><span>planned wire 1.2 · eval_scores append · history/*.eval.ndjson · scorecard.json · .opchain/hindsight · .opchain/rules</span></div>
         </div>
       </details>
@@ -1001,7 +1001,7 @@
             <div class="ma-li-swatch" style="background:color-mix(in srgb, var(--ma-learn) 14%, transparent);border:1.5px dashed var(--ma-learn)"></div>
             <div class="ma-li-name" style="color:var(--ma-learn)">v2.0 learning rail (planned)</div>
           </div>
-          <div class="ma-li-desc">Dashed chrome in the 2.0 "Slate &amp; Emerald" palette marks work that has not shipped — the catalog today is 33 skills at 1.9.1 on wire 1.1. v2.0 closes a loop: wire 1.2 accumulates eval scores, a kit turns them into a decaying directive, and two governed workflows turn outcomes into lessons and rules. The ★ gates are human gates, not audit gates.</div>
+          <div class="ma-li-desc">Dashed chrome in the 2.0 "Slate &amp; Emerald" palette marks work that has not shipped — the catalog today is 33 skills at 1.9.2 on wire 1.1. v2.0 closes a loop: wire 1.2 accumulates eval scores, a kit turns them into a decaying directive, and two governed workflows turn outcomes into lessons and rules. The ★ gates are human gates, not audit gates.</div>
           <div class="ma-li-eg">oc-hindsight · oc-evolve · Outcome Scorecard kit · wire 1.2</div>
         </div>
       </div>
--- a/site/src/components/PipelineDiagram.astro
+++ b/site/src/components/PipelineDiagram.astro
@@ -7,7 +7,7 @@
 // to v1.9 ("Assurance and governed delivery ops") plus the
 // PLANNED v2.0 learning row ("The self-improving pipeline"), which has NOT
 // shipped: there is no skills/oc-hindsight or skills/oc-evolve in the catalog,
-// which is still 33 skills at 1.9.1. Planned nodes are drawn dashed and are
+// which is still 33 skills at 1.9.2. Planned nodes are drawn dashed and are
 // deliberately NOT wrapped in a /skills/<id> link, because those pages do not
 // exist. What is left out on purpose is listed below.
 /**
--- a/site/tests/e2e/changelog-and-scenarios.spec.ts
+++ b/site/tests/e2e/changelog-and-scenarios.spec.ts
@@ -61,10 +61,10 @@
     // with its version + a non-empty compatibility note (changelog-recipe rule).
     const hero = page.locator("#v1-9.hero-card--released");
     await expect(hero).toBeVisible();
-    await expect(hero.locator(".hero-ver")).toContainText("v1.9.0 → v1.9.1");
+    await expect(hero.locator(".hero-ver")).toContainText("v1.9.0 → v1.9.2");
 
     // A patch never creates a new hero: v1.9.1 is a compact rel-card beside it.
-    await expect(page.locator("#v1-9-1.rel-card")).toBeVisible();
+    await expect(page.locator("#v1-9-2.rel-card")).toBeVisible();
     await expect(hero.locator(".hero-head")).toHaveAttribute("aria-expanded", "true");
     await expect(hero.locator(".compat-box")).toBeVisible();
     await expect(hero.locator(".compat-box")).not.toBeEmpty();
