# Generated by oc-security-hardening `/oc-harden fix` + `/oc-harden gate`.
version: 1
controls:
  - id: api.mcp-checkpoint-state
    control: "Hosted MCP checkpoint TTL, 64 KiB cap, bounded requests, strict Origin validation, edge-limited mutations, catalog-only skills, and signed server-issued session isolation"
    applied_in: "src/index.js + src/lib/mcp/server.js + wrangler.jsonc"
    applied: 2026-09-02
    source_finding: "2026-08-22 OSS readiness audit PX-01 and v1.9 Sprint 3 release plan"
    verify:
      - method: test
        cmd: "npm run test:mcp-hardening"
      - method: config
        path: "wrangler.jsonc"
        contains: '"MCP_WRITE_RATE_LIMITER"'
      - method: config
        path: "src/index.js"
        contains: "MCP_SESSION_SIGNING_KEY"
      - method: http
        url: "/mcp"
        request_method: POST
        status: 200
        response_shape: "mcp-session-token"
        json:
          jsonrpc: "2.0"
          id: 1
          method: "tools/call"
          params:
            name: "create_checkpoint_session"
            arguments: {}
  - id: data.lead-retention
    control: "Email-capture KV records expire after 365 days"
    applied_in: "src/index.js"
    applied: 2026-09-02
    source_finding: "2026-08-22 OSS readiness audit PX-02"
    verify:
      method: test
      cmd: "npm run test:data-retention"
