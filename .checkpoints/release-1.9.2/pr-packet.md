Show the tagged 1.9.2 release on the staging candidate: extend the v1.9 changelog range, add the Verified handoffs patch card, recount 20 shipped cards, and align the styleguide and architecture version prose. The product release is merged in #536 and signed tag v1.9.2 is verified on origin.

## Verification

Release-surface checks pass. All 22 focused changelog/scenario browser tests pass against a build using the real GitHub roadmap (12 items). Product runtime code is unchanged from the reviewed tag; the final site commit receives its own executable checks and required CI before staging.

## Documentation

**Generated by:** oc-docs-forge
**Docs changed:** Changelog, styleguide, desktop/mobile architecture version prose, and matching browser assertions.
**Reader impact:** The 1.9.2 card explains repository enrollment and migration requirements and the limits of local hooks, hosted state and execution kits.
**Follow-up docs:** Actual staging verification and monitoring evidence follow deployment. Production promotion is a separate action.
