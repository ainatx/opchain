Ship the three Ember brand surfaces that v2.0.0 (5f06afd) was cut without: the favicon, the fallback share card, and the /architecture dark panel wash.

## Documentation

**Generated by:** oc-docs-forge
**Docs changed:** `design-previews/color-2.0-tooling/MIGRATION-SURFACE.md` addendum (why the census missed these surfaces, what to scan next time). No README, runbook, or skill doc describes favicon/OG generation, so nothing else is stale.
**Reader impact:** Browser tabs, the JSON-LD logo, and link unfurls for /changelog, /dashboard, /404 and other routes without their own card now show Slate & Emerald. `npm run gen-og` (already in prebuild) now also writes `site/public/og-image.png` from `favicon.svg`; recolouring the mark recolours the fallback.
**Follow-up docs:** Record the deployment in `.github/monitoring/release-baseline.json` per `docs/runbooks/cloudflare-challenge.md` if this ships as a post-release hotfix. `/previews/mobile-demo/` and `/previews/compare-rfc/` remain public, archived Ember explorations.

## Repo readiness (oc-repo-ops, pr-readiness-v1)

- Working tree clean; no untracked PR-related files.
- Generated artifacts in sync: `node scripts/gen-og-images.mjs` reproduces `og-image.png` and the 41 `/og` cards byte-identical.
- Catalog surfaces untouched (no `skills/`, registry, or plugin changes); `sync-bundles:check` passed inside the build gate.
- Every path referenced by the docs addendum exists. `.gitignore` unchanged.

## Verification

- Commit-boundary verifier 8/8 PASS (type_safety, lint, tests, anti_patterns, secrets, build, dependencies, dependencies_site).
- Playwright e2e: 118 passed, 1 skipped.
- Pixel scan: 0% warm-hue pixels across all 42 PNGs under `site/public`.
