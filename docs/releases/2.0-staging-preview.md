# 2.0 staging preview

Verified at 2026-09-14T00:42:08.852Z. No release date is set.

| Surface | Verified result |
|---|---|
| Staging | https://staging.opchain.dev |
| Source commit | `3bc8cf380ae3b57033099f7b47de1771fba05e77` |
| Catalog | 2.0.0, 36 skills, 16 registered plugin commands |
| Shared runtime | 2.0-preview.2 |
| Download | 457 files, 11,910,219 bytes; SHA-256 verified against the descriptor |
| Download SHA-256 | `8efabde37e770fdada6601a7b09dca8ac58ebaa9af19711159f4588916568f74` |
| Cloudflare deployment | `dc18ff55-a797-40b4-a4cb-f4bf1eae12a3` |
| Cloudflare version | `befa8713-c096-4bf4-a933-9e4010b2730c`, 100% traffic |
| Rollback version | `a9cc0fdd-4c9d-4064-be4c-86ddfc88300c` |
| Production | Unchanged at `f5e4b2a` |

## Completed checks

All eight candidate checks passed: types, references, tests, built in code and
secret scans, build, root dependencies and site dependencies. The commit hook
repeated its required checks. Typed code and security audit evidence passed
before deployment. The staging wrapper passed its clean tree checks, hardening
replay, exact live SHA check and full smoke suite.

The live catalog and update bundle report 2.0.0 and contain all 36 skills.
The downloaded content matches its SHA-256. The preview banner is visible.
Browser execution reported no page errors. The live dark background is
`#12191f`, the light background is `#e9f0f7`, and the corresponding accent colors
are `#2be179` and `#0d7a3e`.

Authenticated control plane checks verified the recorded deployment, version,
100% traffic, script fingerprint, bindings, custom domain and observability for
both staging and unchanged production at 2026-09-14T00:43:06.633Z.

## Monitoring and rollback

The release branch records the new staging entry in
`.github/monitoring/release-baseline.json`. The production baseline is preserved.
Scheduled monitors use the default branch, so a separate reviewed baseline update
must reach that branch before they recognize this preview. This record does not
claim scheduled monitoring has already adopted it.

If this preview needs to be removed, restore its captured staging version:

```sh
npx wrangler rollback a9cc0fdd-4c9d-4064-be4c-86ddfc88300c --env staging --yes --message "restore staging before 2.0 preview"
```

Then verify staging health and refresh its monitoring baseline to match.

## Still required for a release

See [runtime alignment](../plans/2026-09-13-v2-runtime-alignment.md) for real
provider acceptance, external reviewer setup, native host acceptance, migration,
repository split and signed release rehearsal. Staging is available for review;
production publication and a 2.0 tag remain outside this task.
