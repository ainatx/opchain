# OSS-split execution handoff — staging review → cut-over

_Operator instructions for an autonomous agent (written for Codex; any competent agent or human can follow them). Written 2026-08-26 against main `b1ba54f`; rev 3 on 2026-08-27 after a 3-agent adversarial verification (38 findings applied — freeze scope, filter-rename collision, roadmap deploy prerequisites, DCO-squash nuance, and the C4 dependency correction among them); rev 4 on 2026-08-28 after #458 changed the release sequencing — **v1.8.3 is being cut from the monorepo BEFORE the flip** (product half merged; signed tag + site half #459 pending), so Phase A gained the tag gate, C3/C4 expect v1.8.3, Phase D became the first *product-repo* release (v1.8.4+), and the mcp-publisher pin shipped in this same PR. **Do not trust this document's snapshot of state — re-verify every "current state" claim in Phase 0 before acting.** The strategy, evidence, and rollback reasoning live in [docs/plans/2026-08-22-oss-split-licensing-compliance.md](../plans/2026-08-22-oss-split-licensing-compliance.md) (the plan; § references below are to it), [docs/plans/2026-08-25-site-surface-pass.md](../plans/2026-08-25-site-surface-pass.md), and [docs/plans/2026-08-26-roadmap-github-issues.md](../plans/2026-08-26-roadmap-github-issues.md). Read all three before Phase C. **Where this document and the plan disagree, this document wins** — it post-dates the seam PRs (S1–S5) and corrects plan passages they superseded; note each such divergence in the PR that acts on it._

_Rev 5 on 2026-08-29 records the evidence-bound v1.8.3 publisher exception discovered during execution: the pinned publisher's `publish --dry-run` performed the real publish, so the following publish failed as a duplicate even though the official registry outcome succeeded. This is a one-version adjudication, not a generic waiver for failed workflows._

_Rev 6 on 2026-08-29 records the option-3 monitoring redesign and the user's Phase C authorization. Branch Canary run [33278930280](https://github.com/asfbay-bit/opchain/actions/runs/33278930280) and Deploy lag run [33278931387](https://github.com/asfbay-bit/opchain/actions/runs/33278931387) validate the authenticated Cloudflare control-plane design, but scheduled default-branch activation remains pending the closeout PR merge. Phase C may proceed through its documented gates; this closeout deliberately stops at the C1 HUMAN manifest review/freeze gate. It does not satisfy C5's exact `flip now` confirmation or authorize a merge, force-push, tag movement, extraction, or new runtime SHA._

## Decisions — already made, do not re-ask

| # | Decision | Value |
|---|---|---|
| D1 | License | **Apache-2.0**, whole monorepo + product repo (shipped 2026-08-24; releases ≤ 1.8.2 were MIT) |
| D2 | Copyright holder / owner | **"Aidan Elsesser and the opchain contributors"**, repos stay under `asfbay-bit` |
| D3 | Monorepo visibility | **Stays public**; forward-only scrub (done); never rewrite its history |
| D4 | Inbound terms | **DCO, no CLA**; the open core is permanently open (whether `opchain-work` needs a relicense option is a human-checklist question, not yours) |

## Hard rules

1. **Never force-push `main` of `ainatx/opchain`.** The one sanctioned force-push in this job is the single cut-over push to `asfbay-bit/opchain-skills` in Phase C5.
2. **Commit identity:** every commit **on a PR branch** is authored `Aidan Elsesser <admin@opchain.dev>` with `git commit -s` (the DCO App checks the PR branch commits and requires the sign-off email to match the author) and a `Co-Authored-By:` trailer naming your agent. Never let the agent be the git author. Know the squash nuance: GitHub's squash-merge authors the landed commit as the GitHub account and keeps trailers only if they survive in the squash message — keep `Signed-off-by:` in the squash message body on the product repo.
3. **Human gates are marked `⛔ HUMAN`.** Stop and wait for Aidan at each one; do not proceed on your own judgment.
4. **Deploys:** staging must normally equal the current `origin/main` tip; `scripts/deploy.mjs` enforces the weaker mechanical floor that the deployed commit is an ancestor. Prod only after a human eyeballs staging **at the same SHA you will ship**. The sole rev-5 documentation/checkpoint adjudication may advance the tip without replacing runtime artifact `395fc314c836d5d9d8a76b64d85b8380ece3a68b`; keep staging and production pinned to that already reviewed/tagged ancestor. `.dev.vars` must exist at repo root before deploying (copy from the primary checkout if absent).
5. **Verification over recall:** before each phase, re-run that phase's "verify" block. If reality disagrees with this doc, reality wins — read the plan, adjust, and note the divergence in the PR/commit that fixes it.
6. **PR flow:** one PR at a time; the repo's PR template, CODEOWNERS review, and CI (Worker, Site, e2e, LHCI) must be green; squash-merge. Update `.checkpoints/*.checkpoint.json` at inflection points (`npm run checkpoint:validate` must pass).
7. Build side-effects: `npm run build` regenerates `site/public/og/*.png` byte-nondeterministically — **never commit OG churn**; `git checkout -- site/public/og` before staging files.

## Phase 0 — Ground truth (run first, every session)

```bash
git fetch origin && git log --oneline -3 origin/main
curl -s "https://opchain.dev/api/health?n=$RANDOM" ; echo          # local point-in-time evidence; may be challenged for some machine traffic
curl -s "https://staging.opchain.dev/api/health?n=$RANDOM" ; echo  # same limitation
node .github/scripts/cloudflare-monitor.mjs control-plane           # requires monitor credentials in the environment
gh pr list -R ainatx/opchain --state open
gh api repos/asfbay-bit/opchain-skills --jq '{pushed_at, default_branch, open_issues_count}'
```

Current v1.8.3 closeout state (verified 2026-08-29): production and staging both report `395fc31` to normal local health traffic. Production deployment `7abc8570-6fcb-4942-b44e-8ef822f84b05` routes 100% to version `780e85e2-7022-44fa-82f3-e5eaeb2b82a1`; staging deployment `8d709a57-181b-4ed7-90cc-ddf773ca14f4` routes 100% to version `c52b3bd6-4eee-4e50-a8c3-e21569b998d6`. Both versions carry the approved script fingerprint. The reviewed runtime SHA remains `395fc314c836d5d9d8a76b64d85b8380ece3a68b`; signed local/origin tag `v1.8.3` object `5ba19cedb72a994dd32d4c4f53e5a962bd68b283` peels to it; and official registry 1.8.3 is active/latest/exact under the accepted one-time publisher disposition. Later `origin/main` is a docs/checkpoint-only descendant, so follow the release-baseline carveout rather than restaging or redeploying it. Reality previously diverged from rev 4's intended ordering: **#459 (the v1.8.3 site half) merged before the tag. Do not replay or revert it.** If `opchain-skills` has *commits* newer than the last mirror run or open *PRs*, **stop — the cut-over plan assumed a disposable snapshot repo; re-assess with Aidan.** Open **issues** on `opchain-skills` are expected: the roadmap files community issues there (12 as of writing — see the roadmap plan doc). They survive the flip (a force-push rewrites git history, not issues), but re-read that doc before C5.

Monitoring closeout state: the old default-branch Canary run `33272073249` and Deploy lag run `33266202606` failed because GitHub-hosted public `/api/health` traffic received Cloudflare managed challenges. Free-plan Bot Fight Mode remains enabled and no narrow WAF Skip exception exists. The option-3 workflows instead authenticate to Cloudflare and validate deployment/version/domain/settings state against `.github/monitoring/release-baseline.json`; Deploy lag uses deploy-relevant-diff semantics, including deletions, so the six intentional docs/checkpoint/workflow/test changes after live `395fc31` do not create false drift. Branch runs `33278930280` and `33278931387` passed at head `9df7e0351ac8fc2676c6c6c8e9bf859a8340e484`, but they intentionally skipped issue mutation and do **not** make scheduled monitoring healthy. Call the state `redesign-validated-pending-merge` until the closeout PR is human-merged and successful default-branch scheduled runs are captured. Assurance limit: control-plane evidence verifies what Cloudflare deployed; it does not prove that public custom-domain machine traffic, including `/mcp`, avoids Bot Fight Mode challenges.

## Phase A — Staging review → prod ship

0. **Sync staging to main first:** normally, if staging `/api/health` version ≠ `git rev-parse --short origin/main`, run `npm run deploy:staging` from a pulled/detached `origin/main` checkout, then start the review. For the rev-5 documentation/checkpoint-only adjudication, do **not** restage its new tip: verify `395fc314c836d5d9d8a76b64d85b8380ece3a68b` remains an ancestor of `origin/main`, then keep staging at `395fc31`, the exact runtime SHA already eyeballed and signed. Never let Aidan eyeball a runtime SHA other than the one you will ship (the 2026-05-13 gap documented in CLAUDE.md was exactly this).
1. Automated review of staging (all must pass):
   ```bash
   npm run smoke:staging
   for p in / /security/ /privacy/ /changelog/ /skills/ /install/ /coverage/ /architecture/; do
     curl -s -o /dev/null -w "%{http_code} $p\n" "https://staging.opchain.dev$p?n=$RANDOM"; done
   curl -s "https://staging.opchain.dev/.well-known/security.txt?n=$RANDOM" | head -3   # GitHub-first Contact + Expires
   curl -s "https://staging.opchain.dev/skills.json?n=$RANDOM" | jq '{license, catalogVersion, count}'
   ```
2. Visual spot-check (screenshots if you can render): `/security` (private-advisory CTA, no form), `/changelog` (Apache-2.0 card **and a populated roadmap/vote panel** — see step 4's roadmap prerequisites), `/showcase` (sample-labeled cost copy), footer license link.
3. `⛔ HUMAN` — Aidan eyeballs staging and says ship.
4. Ship. **Release-tag gate first (#458's machinery):** the lockstep catalog on main says 1.8.3, and `deploy.mjs` refuses a production deploy until a pushed, signature-verified `v1.8.3` tag is an ancestor of HEAD, is the same raw tag object on origin, and carries the reviewed `release-seal.json`. That seal binds the catalog version, exact publisher-workflow SHA-256, and exact `server.json` registry payload: ancestry + version/count alone still accepts `438ab5f`, where all 29 skills already said 1.8.3 but the publisher remained unsafe, while a copy-only seal cannot bless its old workflow or another registry target. The original rev 4 command named that #458 merge SHA; a tag-triggered workflow is loaded from the tagged ref, so it would run the unpinned `releases/latest` downloader with `id-token: write`. **Never tag `438ab5f` or another commit that lacks #460 and the 1.8.3 release seal.** Instead, after all release-reconciliation changes are merged, resolve one reviewed `origin/main` SHA that contains #458, #459, #460, and the seal; verify the workflow at that exact object (`git show <release-sha>:.github/workflows/publish-mcp-registry.yml`) is SHA-pinned, checksum-verifies the fixed `mcp-publisher` version, and hashes to the seal; verify `server.json` hashes to the seal too; deploy that same SHA to staging and complete steps 1–3. Then `⛔ HUMAN` — Aidan creates the **signed** tag and runs the local gate **before the push can trigger OIDC publishing**: `git tag -s v1.8.3 <release-sha>` → `node scripts/check-release-tag.mjs --local` → `git push origin v1.8.3` → `node scripts/check-release-tag.mjs`. #459 is already merged; this is a recorded sequencing exception, not a step to repeat. Only after the tag workflow succeeds and every remaining prerequisite below is satisfied may that same SHA go to production:
   **One-time v1.8.3 publisher disposition — not a generic failed-workflow waiver:** tag object `5ba19cedb72a994dd32d4c4f53e5a962bd68b283` peels to the reviewed/staged commit `395fc314c836d5d9d8a76b64d85b8380ece3a68b`. In tag run [33234878843](https://github.com/asfbay-bit/opchain/actions/runs/33234878843), the successful `Validate` step ran `mcp-publisher publish --dry-run` and created official registry version 1.8.3 at `2026-08-29T04:53:31.36821Z`; the only failed step was the immediately following publish, which returned `400 ... cannot publish duplicate version`. Phase A may treat this specific publication outcome as satisfying the publisher gate only after all of these checks pass: the local/origin signed-tag gates pass; the tag object and peeled commit equal the values above; the official `versions/1.8.3` server record matches tagged `server.json`; registry status is `active` and `isLatest` is true; `publishedAt` falls inside the successful `Validate` step; the second duplicate publish is the run's only failure; and `⛔ HUMAN` Aidan explicitly accepts this recorded exception. Accepting it satisfies **only** the publisher gate; it does not accept the deferred HIGH, authorize production, or satisfy the separate production HUMAN gate. **Do not rerun the workflow or delete, move, or replace the `asfbay-bit/opchain` tag.** This documentation/checkpoint adjudication may advance `main`, but it is not a replacement runtime artifact: production must still deploy detached commit `395fc314c836d5d9d8a76b64d85b8380ece3a68b`, the exact SHA staged, eyeballed, sealed, and tagged above.
   - `⛔ HUMAN` — ensure `ROADMAP_GITHUB_TOKEN` exists in **both** environments (fine-grained PAT, issues:write on `opchain-skills`) or the community-roadmap write path 503s `not_configured`. Rev-5 preflight found the production secret name present and the staging secret name absent, so Aidan must run `wrangler secret put ROADMAP_GITHUB_TOKEN --env staging`, then verify both by name without printing either value.
   - Vote-history KV: the roadmap plan prescribes copying applicable `vote-count:` keys to the new numeric-issue-number keys at deploy time. Rev-5 preflight found the prescribed legacy sources `OPC-170`/`OPC-173`/`OPC-174` absent, while the remaining `OPC-*` keys belong to already-shipped roadmap items and have no correct numeric destination; record **migration skipped — no applicable legacy counts**, and leave those unrelated historical keys untouched. If the applicable sources appear before deploy, do the `wrangler kv` copies against the prod `NOTIFY` namespace or record an explicit reset decision — don't leave it undecided.
   - `⛔ HUMAN` — Aidan explicitly accepts the deferred unauthenticated MCP-checkpoint HIGH for **production** and separately says "ship prod" for exact SHA `395fc314c836d5d9d8a76b64d85b8380ece3a68b`. Publisher-exception acceptance and the earlier staging approval satisfy neither of these production decisions.
   - Build with the roadmap data present: `npm run gen-roadmap` immediately before `npm run deploy` (`site/src/data/roadmap.json` is gitignored; a fresh checkout otherwise builds the empty-roadmap fallback).
   Then: from the exact reviewed release checkout (detached is fine): `npm run gen-roadmap && npm run deploy` → `npm run smoke:prod` → verify prod `/api/health` version equals the deployed release SHA (`395fc31` for this rev-5 disposition, not the later documentation-only `origin/main` tip), `/LICENSE` returns 200 with the Apache-2.0 text (don't assert content-type — the ASSETS binding may serve extensionless files as octet-stream), `security.txt` is GitHub-first, and `/changelog` renders the vote targets. Record the exact Cloudflare deployment/version/fingerprint in `.github/monitoring/release-baseline.json` and pass its control-plane + deploy-diff checks; the default-branch workflow, once active, reconciles any deploy-lag issue.

## Phase B — `/privacy` + `/security` compliance pass (independent of the flip; do while waiting on gates)

Rewrite `site/src/pages/privacy.astro` so every claim matches `src/index.js` at time of writing the PR. Required content (verify each against code, cite nothing you didn't read):

- **Remove** all Try-It copy and the 365-day `LEAD_TTL_DAYS` claim; delete the dead `LEAD_TTL_DAYS` type in `site/src/env.d.ts`.
- **Disclose every store with its real retention:** `lead:` records from `/api/notify` (email + metadata; check whether a TTL has been added — if not, either add `expirationTtl` in the same PR or state "retained until deleted on request"); roadmap vote counts + 25h IP-hash locks; `mcp-checkpoint:*` KV (client-supplied JSON, no TTL today — disclose as-is, do NOT claim planned fixes); the `oc_id` cookie; server-side PostHog capture (env-gated, not consent-gated — fix the wording here and on `/security`); client PostHog (consent-gated); feedback → Linear; roadmap feature requests → GitHub issues on `opchain-skills` (public!).
- **`/security` scope fix in the same PR family:** `security.astro` never mentions `POST /mcp` or `/api/votes` — add both to the disclosure scope/KV rows so safe harbor covers the highest-risk surface (plan §4.3).
- Footer claims a lead "unsubscribe anytime" — no unsubscribe mechanism exists; fix the copy or add the path in this PR.
- Add a last-updated stamp, a deletion contact, and `aria-live` on any status region touched.
- Gate: `astro check` 0 errors, `astro build` green, full `npm test` green, then PR per the hard rules. Acceptance: someone reading only the page and only the Worker source finds zero contradictions.

## Phase C — The flip (plan §2.4–§2.5 as amended by S1–S5 and this doc; §5 is the rollback table)

Closeout-branch boundary: the user authorized Phase C, and this branch performs the reversible C0 prerequisites plus prepares C1 for review. Stop at C1's HUMAN manifest review/freeze gate. Do not begin C2 extraction, disable the mirror, push rewritten history or tags, merge a PR, or deploy another runtime SHA from this closeout. C5 separately requires Aidan to say exactly `flip now`.

**C0 — Prereqs.** `brew install git-filter-repo gitleaks`. Close stale drafts #385/#363 if still open. **`publish-mcp-registry.yml` hardening: DONE in the rev-4 PR** (mcp-publisher pinned to an exact version with `sha256sum -c` verification; `actions/checkout` SHA-pinned) — verify it is still pinned before any `v*` tag push or the extraction, since that workflow runs with `id-token: write` and moves into the product repo at the flip. **Scrub `.opchain/pm.yaml`** in the same PR (plan §2.2 requires it to enter the product repo as a fixture: strip the Linear team/project UUIDs and the ADEV ticket plan). The freeze (below) waits until C1's manifest exists, because the freeze scope IS the manifest.

C0 closeout evidence: `git-filter-repo` 2.47.0 and `gitleaks` 8.30.1 are installed; stale drafts #385 and #363 were verified not to touch product paths and closed; the publisher workflow remains version-pinned, checksum-verified, and SHA-pins checkout; `.opchain/pm.yaml` is a neutral fixture; and real `LICENSE`/`NOTICE` copies now live inside `plugins/opchain/` and `mcp/`. These file changes are pending the closeout PR merge.

**C1 — Manifest.** Create `split/product-paths.txt` in the monorepo from plan §2.2, re-derived against the current tree. Beyond the plan's table:
- New seam scripts: `sync-plugin-skills.mjs` and `lint-internal-refs.mjs` are product-pure and **move**; `check-skill-flags.mjs` imports the site flag registry and **stays**.
- #458's release machinery: `scripts/check-release-tag.mjs` reads the lockstep catalog + git tags and is consumed by BOTH the site's `deploy.mjs` prod gate and `/oc-release verify` — treat it like `frontmatter.mjs` (**dual-homed**: in the manifest AND kept in the site repo); add root `release-seal.json` to the product manifest so rewritten release tags retain their reviewed baseline; `release-ledger.yml` is monorepo backstop tooling and **stays**; the `/oc-git-release` verb lives inside `skills/oc-git-ops/` and moves with the product automatically.
- `scripts/lib/frontmatter.mjs` is **dual-homed**: it goes in the manifest (the product's validators and `mcp/local-server.mjs` need it at runtime) **and stays in the site repo** — C6 must NOT `git rm` it, because `check-skill-flags.mjs:14` imports it. Same for `scripts/install-git-drivers.mjs` (extraction copies it; the site keeps its own).
- Governance files: add root `SECURITY.md`, `CODE_OF_CONDUCT.md`, `TRADEMARKS.md`, `NOTICE`, `LICENSES/` explicitly. **CONTRIBUTING.md precedence:** the root file wins; therefore C2 must NOT rename `mirror/CONTRIBUTING.md` (collision — two sources mapping to one destination makes filter-repo refuse or pick arbitrarily); same logic for `mirror/SECURITY.md` (root `SECURITY.md` supersedes it — exclude the mirror copy from the manifest).
PR the manifest for `⛔ HUMAN` review — the filter run is one-shot. **Then** `⛔ HUMAN` — Aidan announces the freeze on *every path in `split/product-paths.txt`* (not just the six directories; the manifest moves ~25 scripts/tests too). Freeze holds until the consume PR merges.

C1 closeout evidence: `split/product-paths.txt` is prepared on the closeout branch with no duplicate or missing paths and no projected rename collision. The process manifest intentionally stays in the site repo rather than selecting itself; `docs/governance/GOVERNANCE.md` and `docs/governance/RELEASING.md` also stay site-side and are copied/adapted during C4. The product-owned checkpoint CLI treats a missing site monitoring baseline as not applicable, so extraction does not introduce a false `doctor --online` warning. The extracted repo still needs a sanitized product `.gitignore` in C4 before product commands run. Human review and the path freeze remain pending; C2 has not started.

**C2 — Extraction** (fresh clone, never the working repo): plan §2.4 steps 2–4 verbatim (`git clone --no-local --single-branch --branch main`, record `EXTRACT_SHA`, optional `--mailmap`), then:
```
git filter-repo --paths-from-file ../split/product-paths.txt \
  --path-rename mirror/README.md:README.md \
  --path-rename mirror/.github/:.github/
```
No `mirror/CONTRIBUTING.md` or `mirror/SECURITY.md` renames (see C1). **Do not use `--replace-text`** (decided: tip-scrub only, blame continuity wins). Skip the plan's own step-5 verify block — C3 below supersedes it (S5 changed the expected symlink state).

**C3 — Verify the rewrite** (all must hold before anything is pushed): commit count is a sanity check, not a gate — measured 83–90 on 2026-08-27 depending on manifest breadth, so expect ≈80–100 and investigate only if wildly outside; `git tag -l` → `v1.8.0 v1.8.1 v1.8.2 v1.8.3` (v1.8.3 was tagged on the monorepo pre-flip per Phase A; its commit bumps all 29 SKILL.md and carries `release-seal.json`, so both survive the filter — if the tag or its seal is absent, stop and reconcile with Phase A); `git ls-files -s | awk '$1=="120000"'` → **empty** (`plugins/opchain/skills` is a real directory since seam S5 — any symlink means stop and re-check); `git log --follow -- skills/oc-app-architect/SKILL.md` reaches 2026-04-14; `README.md`/`CONTRIBUTING.md`/`SECURITY.md`/`.github/ISSUE_TEMPLATE`/`LICENSE`/`NOTICE`/`LICENSES/`/`release-seal.json` at root; `gitleaks git -v .` clean.

**C4 — Bootstrap commit** (one commit, `-s`, human author). Plan §2.4 step 6 is the base, **but its `package.json` block is stale (predates #443) — this paragraph wins:** product `package.json` = `name: opchain-skills, version: 1.8.3, license: Apache-2.0, engines: {node: ">=22"}, dependencies: js-yaml` (version matches the lockstep catalog — v1.8.3 shipped monorepo-side via #458 before the flip) (**not gray-matter** — #443 replaced it with the in-repo `scripts/lib/frontmatter.mjs`, whose dependency — shared with `gen-stack-packs`, `gen-api-dev-adapters` and `pack-dispatch`; `pm-mcp-checks` is zero-import — is `js-yaml`; it must be a runtime dep because `mcp/local-server.mjs` pulls it transitively), `devDependencies: vitest` only. CI with job ids `validate` / `test` / `hooks` / `plugin-layout` (these exact names — the ruleset requires them; **implementations are specified in `docs/audits/2026-08-22-oss-readiness-audit.md`, contributor-experience recommendation (a): JOB 1–4** — `plugin-layout` = parse `marketplace.json`, assert the source dir holds `plugin.json` + `hooks/hooks.json` + `commands/*.md` + a `skills/` tree with one dir per skill). `scripts/check-version-lockstep.mjs` **does not exist yet** — author it per plan §2.6 here, or explicitly defer it to Phase D. `dco.yml`. Governance: `CODE_OF_CONDUCT.md`/`TRADEMARKS.md`/`SECURITY.md`/`NOTICE`/`LICENSES/` carry over from the monorepo root, **but five §4.2 files must land here.** Two have reviewed monorepo drafts as of 2026-08-27 — **copy `docs/governance/GOVERNANCE.md` and `docs/governance/RELEASING.md` to the product root and adapt the two-repo wording** (their status headers say so; GOVERNANCE.md's Maintainers table satisfies `MAINTAINERS.md`, or split it out; the decision record is `docs/plans/2026-08-27-release-surface-governance.md`). The remaining two exist nowhere and must be AUTHORED fresh: `SKILL-AUTHORING.md` (reconcile the `gen-skills-catalog.mjs` and site `content.config.ts` validators into one written contract), `NEW-SKILL-CHECKLIST.md`. Rewrite README/CONTRIBUTING (kill "force-push snapshot / private upstream / PRs can't merge"); regenerate the skill table (**one row per skill dir — 29 today**, the old mirror table had 18) and the issue-template dropdowns from the skill dirs; update `.github/ISSUE_TEMPLATE/config.yml` contacts (add the security-advisories link and a "site/Worker issues belong in ainatx/opchain" redirect). CHANGELOG note "≤1.8.2 MIT; ≥1.8.3 Apache-2.0". Then locally: `npm test`, `claude plugin validate ./plugins/opchain` if available (else validate `marketplace.json`/`plugin.json` shape by hand), and a local-marketplace install test if a Claude Code CLI is present.

**C5 — Cut-over.** `⛔ HUMAN` — Aidan confirms "flip now". Then, in this order: disable the mirror **instantly, without a commit**: `gh workflow disable "Mirror skills to public repo" -R ainatx/opchain` (a stray push would otherwise overwrite the new history; the file + `MIRROR_TOKEN` stay for rollback); `git push --force origin main` to `opchain-skills`; immediately disable `Publish to MCP Registry` in `opchain-skills` **before pushing any carried tags**. Historical tag events load their historical workflow definitions, so the current pin and monotonicity guard do not protect v1.8.0–v1.8.2. Push all four carried tags while publishing is disabled, verify no publisher run started, then re-enable it. **Do not dispatch 1.8.3 again:** verify the existing official 1.8.3 registry record remains active/latest and exactly matches tagged `server.json`, per the Phase A disposition above. Before the first post-cut-over release (v1.8.4+), replace `publish --dry-run` with the publisher's supported validation command, record the repaired workflow hash in that next version's new release seal, and create that next version's signed tag; do not change or replace the immutable v1.8.3 seal/tag after the fact. Apply repo settings + ruleset per plan §2.4 step 9 (contexts = the CI job ids + `DCO`; bypass `pull_request`, never `always`; `web_commit_signoff_required=true`). `⛔ HUMAN` — install the DCO GitHub App on `opchain-skills` (UI-only).

**C6 — Consume PR** in the monorepo, per plan §2.5 **as amended by S1–S5** (the plan's per-file link repoints and its "gen-catalog keeps the flag check" note are superseded — links live in `site/src/lib/repo.ts`, and S2 already split `check-skill-flags` into its own pretest step): remove the manifest paths **except the dual-homed `scripts/lib/frontmatter.mjs` and `scripts/install-git-drivers.mjs`**; `git submodule add` `opchain-skills` pinned to the bootstrap SHA; top-level `skills` symlink; **export `OPCHAIN_SKILLS_DIR=opchain-skills/skills` in the site build path** (`build-site.sh`/`deploy.mjs`) — the symlink alone satisfies Astro's glob but silently kills the per-skill "Updated" badge (`skills.ts` only takes its badge-hiding vendored-tree branch when the env var is set; without it, `git log` on a symlinked path returns empty and the badge vanishes unannounced); rewire `package.json` scripts to the submodule paths, and update the site-owned `check-release-tag.mjs` to validate the product submodule's tag + `release-seal.json` rather than looking for future tags in the site repo; `src/index.js` MCP import path; `actions/checkout` gets `submodules: true` everywhere; npm `prepare` runs submodule init; `deploy.mjs` gains the submodule-drift refusal **and extends its "HEAD reachable from origin/main" refusal to production deploys** (prod ran off-main `c14936d` for two weeks — audit AF-4); Dependabot `gitsubmodule` entry; repoint `site/src/lib/repo.ts` (product issues/advisories → `opchain-skills`); verify `security.txt` still carries both `Acknowledgments` lines (shipped in #440/#442 — verify, don't re-add); rewrite CLAUDE.md's mirror section → "Consuming opchain-skills" **and root `CONTRIBUTING.md`'s PR-routing section** (skills PRs now go to `opchain-skills`; also document the npm-`prepare` git-config mutation and the repo-local `.claude` hooks per §4.2); add a minimal monorepo `.github/ISSUE_TEMPLATE/config.yml` pointing product issues at `opchain-skills`. Ancestry check before opening: `git log --oneline <EXTRACT_SHA>..origin/main -- $(grep -v '^#' split/product-paths.txt)` **must be empty** (the whole manifest, not six paths). CI green → `⛔ HUMAN` review → squash-merge.

**C7 — Cut-over verification** (all must pass): fresh-dir `/plugin marketplace add asfbay-bit/opchain-skills` + `/plugin install opchain` → hooks + 29 skills — **if no interactive `claude` CLI is available, verify the plugin-layout assertions by hand and hand the live install test to Aidan as part of the eyeball; do not mark C7 green on layout checks alone**; `npm ci && npm test && npm run build` green in the monorepo; a fresh `git worktree add` + `npm ci` populates `opchain-skills/` (the prepare hook works); deploy staging → `npm run smoke:staging` + `/skills.json` count 29 + `POST /mcp` list_skills + `/llms.txt` 200 + `/opchain-skills.zip` contains LICENSE; run the 2026-07-04 portability simulation (fresh machine, trace every documented command) as the final acceptance gate; `⛔ HUMAN` eyeball → deploy prod (with the Phase A roadmap prerequisites) → `smoke:prod` → refresh and validate `.github/monitoring/release-baseline.json` for the new exact C7 runtime. Do not call continuous monitoring healthy until that baseline is merged and a scheduled default-branch run succeeds.

**C8 — Retire** (only after C7 is fully green): delete `mirror-public.yml` + `mirror/` in a monorepo PR; `gh secret delete MIRROR_TOKEN` on both repos; `⛔ HUMAN` — revoke the fine-grained PAT in GitHub settings. **Retain the `CLOUDFLARE_ACCOUNT_ID` monitor secret and rotate `CLOUDFLARE_API_TOKEN` to a dedicated least-privilege credential that can read Workers deployments, versions/settings, and custom domains**; the redesigned Canary and Deploy lag workflows use them and C8 must not delete them.

**Rollback:** any step before C5 → delete the scratch clone. C5 itself → `gh workflow enable "Mirror skills to public repo"` **then** `gh workflow run mirror-public.yml` (a disabled workflow can't be dispatched — enable first; this is why the file + token survive until C8). C6 → revert the PR. Never roll back by force-pushing the monorepo.

## Phase D — First product-repo release: `v1.8.4` or `v1.9.0` (plan §2.6)

**v1.8.3 already shipped monorepo-side pre-flip** (#458 product half + signed tag per Phase A + #459 site half), so the first release cut from `opchain-skills` is the NEXT version. `docs/governance/RELEASING.md` (copied to the product root in C4) governs the process, including its §6 mandatory surface audit.

Two-PR flow. **Product half:** before any new tag, replace the publisher's `publish --dry-run` validation step with its supported validation command and add regression coverage; bump 29× `SKILL.md version:` + `skills/CHANGELOG.md` + `plugin.json` + `marketplace.json` ×2 + `server.json` + a new `release-seal.json` that records the repaired workflow hash; plan §2.6 housekeeping lands here — rework `skills/oc-release-ops/references/version-locations.md` (stale v1.3 examples; omits the three manifests) and `oc-release-ops/SKILL.md`'s bump procedure for the two-repo flow, and wire `scripts/check-version-lockstep.mjs` into product CI; tag the new version — **signed and annotated (`git tag -s`)** per plan §4.1 row 18 and RELEASING.md; push from main → the repaired, pinned, monotonicity-guarded `publish-mcp-registry.yml` runs via OIDC; add `release.yml` (needs `permissions: id-token: write, attestations: write`) attaching `opchain-skills.zip` + `SHA256SUMS` + SBOM + `actions/attest-build-provenance`. **Add the SHA-pinned enterprise install entry** to `marketplace.json` — a second plugin entry with a `git-subdir` source (`url` + `path: plugins/opchain` + `ref: <the new tag>` + `sha`) or an `archive` source with `sha256` of the release zip; **validate it against the marketplace `$schema` and `claude plugin validate` before tagging** — if the current CLI rejects the source shape, fall back to the other form or file upstream; the default relative-path entry stays. **Site half:** bump the submodule pin + the 8 live-claim surfaces (`scripts/check-release-surfaces.mjs` verifies; `check-release-tag.mjs` requires the tag) + the `changelog.astro` entry → staging → `⛔ HUMAN` eyeball → prod (with roadmap prerequisites). `/skills.json catalogVersion` must then report the new version. Never create `v*` tags in the site repo again.

## Phase E — Remaining board (after the flip; each its own PR)

1. Surface-pass "now" leftovers ([2026-08-25-site-surface-pass.md](../plans/2026-08-25-site-surface-pass.md)): the D5 `aria-live` sweep, D6 token-debt sweep (undefined `--dim`, `/dashboard` foreign token vocabulary — light theme unreadable), `/pipeline-builder` decision-table refresh, `did.json` (mint via `scripts/gen-did.mjs` locally — key material never in CI — or drop the `host.identifier`), skills.ts role map completeness check, **D3 license-visibility remnants** (JSON-LD `license` key in Base/index/skill nodes, `<link rel=license>`, skill-page license badge, download-CTA microcopy). `roadmap-static.ts` is already deleted (#449) — skip; note CLAUDE.md still references it.
2. Hosted-service data handling (plan §4.3): TTL + size cap + session isolation + rate limit on `POST /mcp write_checkpoint`; leads TTL; `DATA-HANDLING.md`; **plus the one-time purge/backfill of pre-existing `lead:` and `mcp-checkpoint:*` keys when the TTL change deploys** (old data must obey the retention the Phase B page describes).
3. Counts/release module + pinned-surfaces test (surface-pass D2) so v1.9 can't recreate the string drift.
4. **Workflow/supply-chain hardening tranche** (plan §4.1 "Must" rows, both repos): SHA-pin every remaining tag-pinned `uses:` (Dependabot's `github-actions` ecosystem keeps them fresh), workflow-level `permissions: contents: read` with job-level escalation, `persist-credentials: false` on checkouts, CodeQL default setup (js/ts + actions), a dependency-review / `npm audit --audit-level=high` CI step, ESLint over `src/ scripts/ plugins/opchain/hooks mcp/`, then the OpenSSF Scorecard action + Best Practices badge. (The `mcp-publisher` pin is NOT deferred here — it's a C0 prerequisite.)
5. **Adopter-trust items** (plan §4.4): give the `/install` SessionStart update-check hook an env opt-out (`OPCHAIN_NO_UPDATE_CHECK=1`) and disclose the phone-home next to it; add the per-hook behaviour-contract table to `plugins/opchain/README.md` (event, stdin, files read/written, subprocesses, network: none, timeout, fail-closed); switch `pre-commit-gate.cjs`'s index scratch to `mkdtemp` mode `0o700`.
6. **Licensing-metadata remnants** (plan §3.2, or record an explicit deferral): SPDX headers + `REUSE.toml` + `reuse lint` CI in the product repo; the dependency-hygiene sweep in the site repo (license-allowlist CI step; declare/remove phantom deps `sharp`/`unist-util-visit`; move `astro`/`marked`/`dompurify` to devDependencies; drop unused `@astrojs/cloudflare`; esbuild-metafile zod-only check); hand-written `plugins/opchain/sbom.spdx.json` with the v1.8.3 release. Also: move `reports/token-usage/` out of the public monorepo to a private archive (plan §2.2 — per-session $ ledger with session/message ids).
7. Later-wave surface rows remain on the surface-pass board and are **not** exhaustively listed here (OSS-split blog post at flip time, ConsentBanner dialog semantics, `/styleguide` tokens, `PipelineDiagram.astro` park/delete, Base.astro skip-link/`twitter:card`/RSS autodiscovery, Header contribute nav).

## Human-only checklist (Aidan)

- [x] Phase A: exact SHA `395fc314c836d5d9d8a76b64d85b8380ece3a68b` sealed, deployed to staging, automatically verified, and eyeballed in Safari
- [x] Phase A: signed `v1.8.3` created and pushed; local/origin tag object and peeled-commit gates pass; official registry 1.8.3 is active/latest/exact
- [x] Phase A: one-time run-33234878843 publisher disposition explicitly accepted; this satisfies only the publisher gate
- [x] Phase A: production `ROADMAP_GITHUB_TOKEN` secret name present; applicable `OPC-170`/`173`/`174` vote sources absent, so migration is recorded skipped and unrelated historical keys remain untouched
- [x] Phase A: staging `ROADMAP_GITHUB_TOKEN` updated and both environments verified by secret name without printing values
- [x] Phase A: deferred MCP checkpoint HIGH accepted for production and exact SHA `395fc314c836d5d9d8a76b64d85b8380ece3a68b` separately approved and shipped
- [ ] Phase C5 "flip now"; C7 eyeball; Phase D eyeball
- [ ] Install the DCO App on `opchain-skills` (C5); revoke the `MIRROR_TOKEN` PAT (C8)
- [x] Monitoring disposition: keep Free-plan Bot Fight Mode enabled, use the authenticated control-plane redesign, and retain the documented residual risk for public machine traffic; no WAF Skip exception exists
- [x] Phase C authorization accepted; reversible C0 prerequisites complete and C1 manifest prepared on the closeout branch
- [ ] Phase C C1: HUMAN manifest review and path-freeze announcement before any extraction
- [ ] Triage the ~6 open Dependabot alerts; decide the Astro 7 PR (#444)
- [ ] Trademark clearance search (OPENCHAIN Reg. 5242152 / OPTCHAIN Reg. 7397498) **before any public announcement** of the contributor repo — full picture incl. the OP Labs "OP CHAIN" ITU blocker in [docs/legal/2026-08-25-opchain-trademark-knockout.md](../legal/2026-08-25-opchain-trademark-knockout.md)
- [ ] Defensive namespace grabs while they're free (verified 2026-08-25): npm `opchain` **and** `opchain-skills`, the `@opchain` npm org scope, PyPI `opchain`, domain `opchain.io`, `opchain.bsky.social` — and keep an eye on **opchain.ai** (unknown party, "Launching Soon" email-capture lander)
- [ ] TSDR docket dates for the OP Labs "OP CHAIN" applications (SN 98122443 / 98122448): check ~2026-10-08, ~2027-04-08, drop-dead 2027-10-08
- [ ] Decide: does opchain.dev need a ToS/AUP page before enterprises point agents at `/mcp` (plan §4.3)? Does `opchain-work` need a relicense option (D4 note)?
- [ ] Name a second maintainer/reviewer, then raise `required_approving_review_count` to 1 on `opchain-skills`
